# ASM Pipeline Redis Configuration
# Exposed on 192.168.1.21:6379

bind 0.0.0.0
protected-mode yes
port 6379
tcp-backlog 65535
timeout 0
tcp-keepalive 300

# Persistence
appendonly yes
appendfilename "appendonly.aof"
appendfsync everysec
no-appendfsync-on-rewrite no
auto-aof-rewrite-percentage 100
auto-aof-rewrite-min-size 64mb

# Memory
maxmemory 2gb
maxmemory-policy allkeys-lru

# Security
requirepass changeme

# Logging
loglevel notice

# Save points (disabled because AOF is enabled)
save ""
